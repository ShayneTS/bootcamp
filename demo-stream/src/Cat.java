// ! Java 16
// ! Attribute (Final by default) 
public record Cat(String name, int age) {

  // Constructor
  // public Cat(String name){}
  // getter 
  // setter (we won't use it. "record" like a history. We don't revise history)

}
